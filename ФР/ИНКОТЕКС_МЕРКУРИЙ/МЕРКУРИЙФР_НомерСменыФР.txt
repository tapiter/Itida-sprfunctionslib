// Номер смены в ФР
IF ( !ПУСТО( ЕСЛИ( ТИП( "НомерСменыККМ" ) == "U", 0, НомерСменыККМ ) ) )
{
	RETURN НомерСменыККМ;
}
НомерСменыККМ = ЗАПРОС( "SELECT ecrsession FROM sprequipment WHERE code = '" + КОДОБОРУДОВАНИЯ + "'" );

RETURN НомерСменыККМ;
