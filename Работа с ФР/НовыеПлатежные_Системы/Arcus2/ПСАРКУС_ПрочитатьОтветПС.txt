ИмяФайлаОтвета = "C:\Arcus2\rc.out";
СТРОКАДАННЫХ = "";
WAIT(500);
IF ( ФАЙЛ(ИмяФайлаОтвета, 0) )
{
	ФайлОтвета	= ФАЙЛОТКРЫТЬ( ИмяФайлаОтвета, 0, 0 );
	ФАЙЛУСТАНОВИТЬУКАЗАТЕЛЬ(ФайлОтвета, 0);
	ДАННЫЕОТВЕТА = ФАЙЛПРОЧИТАТЬ(ФайлОтвета, "B" );	
	ФАЙЛЗАКРЫТЬ( ФайлОтвета );
	
	СТРОКАДАННЫХ = СТРОКАИЗМАССИВА(	ПЕРЕКОДИРОВАТЬ( ДАННЫЕОТВЕТА, "WINDOWS-1251", "UTF-16") );
	
	СТРОКАДАННЫХ = ALLTRIM( STRTRANC( STRTRANC( СТРОКАДАННЫХ, CHR(13), "" ), CHR(10), "" ) );
}

RETURN СТРОКАДАННЫХ;
