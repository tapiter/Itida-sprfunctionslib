Сертификат			= СТРОЧНЫЕ( Сертификат );

СПИСОКСЕРТИФИКАТОВ( "ВременныйСписокСертификатов", "MY" );
ПОКА ( !КОНЕЦКОНТЕКСТА( "ВременныйСписокСертификатов" ) )
{
	IF ( СТРОЧНЫЕ( ВременныйСписокСертификатов.certid ) == Сертификат )
	{
		Отпечаток	= ПОДСТРОКА( ВременныйСписокСертификатов.thumbprint, 3 );
		УДАЛИТЬКОНТЕКСТ( "ВременныйСписокСертификатов" );
		RETURN Отпечаток;
	}
	ПРОПУСТИТЬ( 1, "ВременныйСписокСертификатов" );
}
УДАЛИТЬКОНТЕКСТ( "ВременныйСписокСертификатов" );
RETURN "";
