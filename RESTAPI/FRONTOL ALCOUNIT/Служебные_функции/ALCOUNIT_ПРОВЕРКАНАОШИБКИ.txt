Результат		= true;
IF ( AT( Ответ, "error" ) > 0 ) 
{
	ЗАГРУЗИТЬJSON( "Ошибка", ПЕРЕКОДИРОВАТЬ( Ответ, "UTF-8", "ANSI" ) );
	Ответ		= "Ошибка: " + ЗНАЧЕНИЕПОЛЯ( "Ошибка", "error" ) + CHR(13) + CHR(10) + "Марки: " + ЗНАЧЕНИЕПОЛЯ( "Ошибка", "stamps" ) + CHR(13) + CHR(10) + "Организация: " +  ЗНАЧЕНИЕПОЛЯ( "Ошибка", "organisations" );
	Результат	= false;
}
RETURN Результат;
