IF ( !ВыгружатьНачальныеОстатки )RETURN "";IF (ВыгружатьТоварыСвернутоПоНДС)RETURN "";IF ( ПУСТО( Фирма ) )	_ФирмаДляОстатков = "'0000001'";ELSE		_ФирмаДляОстатков = "(SELECT CASE WHEN ex_code = '' THEN code ELSE ex_code END FROM sprfirm WHERE code = '" + Фирма + "')";		ТЕКСТЗАПРОСА = "SELECT						'" + TTOC( ДОБАВИТЬДНИ(Период_min, -1), 7, ".", "T", ":")+"'		AS Date,			" + _ФирмаДляОстатков + "					AS Организация,			'Товары'		 							AS РазделУчета, 			'перенос начальных остатков из ПП Айтида'	AS Комментарий,			CONVERT( bit, 1 )							AS ОтражатьВБухгалтерскомУчете,			CONVERT( bit, 1 )							AS ОтражатьВНалоговомУчете,			CONVERT( bit, 1 )							AS ОтражатьПоСпециальнымРегистрам,			CONVERT( bit, 1 )							AS РучнаяКорректировка";		 RETURN ТЕКСТЗАПРОСА;
