Меркурий.ЗагрузитьПараметры( );IF ( !Меркурий.ЗапросСправочникаСтран( ) ) RETURN false;IF ( !Меркурий.ЗапросСправочникаТиповНазначенийГрузов( ) ) RETURN false;IF ( !Меркурий.ЗапросСправочникаЕдиницИзмерения( ) ) RETURN false;
