// Отмена чека
IF ( !ФР.ШТРИХ_ОткрытьФР( КОДОБОРУДОВАНИЯ, "Отмена чека" ) ) RETURN false;

driverKKM.Password					= FROperatorProfile; // Пароль
_ERROR				= driverKKM.SysAdminCancelCheck( ) <> 0;
 
/*
IF (ВЫБРАТЬКОНТЕКСТ( "РезультатыОбработкиКМ" ))
{
	УДАЛИТЬКОНТЕКСТ( "РезультатыОбработкиКМ" );
	ФР.ШТРИХ_ОчиститьТаблицуПроверенныхКМ( КОДОБОРУДОВАНИЯ );
}
*/

RETURN ФР.ШТРИХ_ЗакрытьФРСПроверкойНаОшибки( "Отмена чека" );
