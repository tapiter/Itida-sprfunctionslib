ИмяФайлаОтвета = "C:\Arcus2\rc.out";
СТРОКАДАННЫХ = "";
IF ( ФАЙЛ(ИмяФайлаОтвета, 0) )
{
	ФайлОтвета	= ФАЙЛОТКРЫТЬ( ИмяФайлаОтвета, 1, 1 );
	ФАЙЛУСТАНОВИТЬУКАЗАТЕЛЬ(ФайлОтвета, 0);
	СТРОКАДАННЫХ = ALLTRIM( ФАЙЛПРОЧИТАТЬ(ФайлОтвета, "S", "" ) );
	ФАЙЛЗАКРЫТЬ( ФайлОтвета );
}

RETURN СТРОКАДАННЫХ;
