ФайлДляОтправки		= ФАЙЛОТКРЫТЬ( "Файлы в формате XML|*.xml|Все файлы|*.*", 2, 0 );
ОтветСервера 		= "";
// Нажали отмену
IF ( ФайлДляОтправки < 0 ) RETURN false;
ТекстЗапроса		= ФАЙЛПРОЧИТАТЬ( ФайлДляОтправки, "S", ФАЙЛРАЗМЕР( ФайлДляОтправки ) );
ФАЙЛЗАКРЫТЬ( ФайлДляОтправки );

// Отправляем получившийся текст на сервер
IF ( !ЕГАИС.HTTPCommand( "POST", "/opt/in/" + КАТАЛОГУТМ, ТекстЗапроса, @ОтветСервера, ИМЯФАЙЛАВУТМ ) ) RETURN false; 
ЕГАИС.ДобавитьЗапрос( 22, "", 0, ТекстЗапроса, ОтветСервера );
СООБЩЕНИЕ( "Запрос успешно отправлен." );
RETURN true;
