// Номер смены в ФР
IF ( !ПУСТО( ЕСЛИ( ТИП( "НомерЧекаККМ" ) == "U", 0, НомерЧекаККМ ) ) )
{
	RETURN НомерЧекаККМ;
}

НомерЧекаККМ = ЗАПРОС( "SELECT chequeNumber FROM sprequipment WHERE code = '" + КОДОБОРУДОВАНИЯ + "'" );

RETURN НомерЧекаККМ;
