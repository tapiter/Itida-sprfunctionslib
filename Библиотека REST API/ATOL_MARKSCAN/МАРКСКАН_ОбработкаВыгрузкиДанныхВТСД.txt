БлочнаяВыгрузка = true;
КоличествоТоваровВБлоке = 500;

_ВЫГРУЖАТЬОСТАТКИ		= true;
_ВЫГРУЖАТЬДОКУМЕНТ		= true;
_ВЫГРУЖАТЬТОВАРЫБЕЗЦЕНЫ = true;

IF ( ТИП( "СПИСОКДОКУМЕНТОВ" ) == "U" ) СПИСОКДОКУМЕНТОВ= "";
СКЛАД				= ЗАПРОС( "SELECT sklad FROM sprequipment WHERE code= '" + КОДОБОРУДОВАНИЯ + "'" );

РезультатВыгрузкиТоваров = RESTAPI.МАРКСКАН_ВыгрузитьТоварыВТСД( КОДОБОРУДОВАНИЯ );
IF ( !РезультатВыгрузкиТоваров )
{
	СООБЩЕНИЕ("Не удалось выгрузить товары в ТСД: " + RESTAPI.ИМЯОБОРУДОВАНИЯ(), RESTAPI.ИМЯОБОРУДОВАНИЯ() );
	RETURN false;
}
	
РезультатВыгрузкиДокументов = RESTAPI.МАРКСКАН_ВыгрузитьДокументыВТСД( КОДОБОРУДОВАНИЯ );
IF ( !РезультатВыгрузкиДокументов )
{
	СООБЩЕНИЕ("Не удалось выгрузить документы в ТСД: " + RESTAPI.ИМЯОБОРУДОВАНИЯ(), RESTAPI.ИМЯОБОРУДОВАНИЯ() );
	RETURN false;
}

RETURN true;
