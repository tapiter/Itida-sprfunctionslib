// Запрашиваем имя файла для загрузкиИДДокумента				= ЗАПРОС( "SELECT ic FROM mark_out WHERE guid = '" + GUIDДокумента + "'" );
ТипДокументаОснования	= ЗАПРОС("SELECT TOP 1 code FROM crosslink WHERE cr_code = '002' AND cr_ic = " + ИДДокумента + " AND cr_type = '0' AND fstorno = 0");
IF (ТипДокументаОснования == "050")
	ИмяФайла				= Маркировка.СоздатьXMLИзДокумента_АКТ( ПРЕДПРИЯТИЕ, GUIDДокумента );
ELSE
	ИмяФайла		= Маркировка.СоздатьXMLИзДокумента( ПРЕДПРИЯТИЕ, GUIDДокумента );
	IF ( ПУСТО( ИмяФайла ) ) RETURN false;ИмяФайлаБезПути	= ИмяФайла;IF ( ATC( ИмяФайла, "\" ) > 0 )	ИмяФайлаБезПути	= ПОДСТРОКА( ИмяФайла, RAT( ИмяФайла, "\" ) + 1 );IF ( ТИП( "_МАРКИРОВКАПУТЬДЛЯСОХРАНЕНИЯФАЙЛОВ" ) == "U" ) 	_МАРКИРОВКАПУТЬДЛЯСОХРАНЕНИЯФАЙЛОВ	= "";	IF ( ПУСТО( _МАРКИРОВКАПУТЬДЛЯСОХРАНЕНИЯФАЙЛОВ ) ){	НовоеИмяФайла						= ФАЙЛВЫБРАТЬ( false, "Файлы XML|*.xml", 526348, "xml", ИмяФайла );	_МАРКИРОВКАПУТЬДЛЯСОХРАНЕНИЯФАЙЛОВ	= ЛЕВСИМВ( НовоеИмяФайла, RAT( НовоеИмяФайла, "\" ) );}IF ( ПРАВСИМВ( _МАРКИРОВКАПУТЬДЛЯСОХРАНЕНИЯФАЙЛОВ, 1 ) != "\" )	_МАРКИРОВКАПУТЬДЛЯСОХРАНЕНИЯФАЙЛОВ	+= "\";НовоеИмяФайла		= _МАРКИРОВКАПУТЬДЛЯСОХРАНЕНИЯФАЙЛОВ + ИмяФайлаБезПути; RETURN ФАЙЛПЕРЕИМЕНОВАТЬ( ИмяФайла, НовоеИмяФайла, 11 );
