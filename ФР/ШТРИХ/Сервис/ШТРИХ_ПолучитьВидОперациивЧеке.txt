IF ( ТипЧекаККМ == 0 ) //приход
	ВидОперации = 1; 
ELSE IF ( ТипЧекаККМ == 2 ) //возврат прихода
	ВидОперации = 2;
ELSE IF ( ТипЧекаККМ == 1 ) //расход
	ВидОперации = 3;
ELSE IF ( ТипЧекаККМ == 3 ) //возврат расхода
	ВидОперации = 4;
ELSE
	ВидОперации = 1; //по умолчанию возвращаем Продажу (приход)
	
RETURN ВидОперации;
