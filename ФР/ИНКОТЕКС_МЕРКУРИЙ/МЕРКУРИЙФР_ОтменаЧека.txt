// Отмена чека
IF ( !ФР.МЕРКУРИЙФР_ОткрытьСессию( КОДОБОРУДОВАНИЯ, "Отмена чека" ) ) RETURN false;

Команда = ДАННЫЕ_JSON("command", "ResetCheck");
ТекстЗапроса = "{ ""sessionKey"": """ + _КЛЮЧ_СЕССИИ + """, " + Команда + " }";

ОТВЕТ = "";

IF ( !ФР.МЕРКУРИЙФР_HTTPPOST(ФР.МЕРКУРИЙФР_АДРЕССЕРВЕРА(), "/api.json", ТекстЗапроса, "", @ОТВЕТ ) )
	RETURN false;
		
RETURN ФР.МЕРКУРИЙФР_ЗакрытьСессию( КОДОБОРУДОВАНИЯ, "Отмена чека" );
