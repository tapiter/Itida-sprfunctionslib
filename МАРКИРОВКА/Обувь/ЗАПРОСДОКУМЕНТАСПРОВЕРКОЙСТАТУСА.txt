// Запрашиваем статус документаДокумент			= МАРКИРОВКА.ЗАПРОСДОКУМЕНТА( ПРЕДПРИЯТИЕ, СЕРТИФИКАТ, ТЕСТОВЫЙКОНТУР, ИДДОКУМЕНТА );IF ( ПУСТО( Документ ) ) RETURN "";ЗАГРУЗИТЬJSON( "Документ", Документ );// Если документ ещё не обработан делаем несколько попытокКоличествоПопыток	= 0;WHILE ( КоличествоПопыток < 10 && ПРОПИСНЫЕ( СЖАТЬПРОБЕЛЫ( ЗНАЧЕНИЕПОЛЯ( "Документ", "status", "" ) ) ) == "IN_PROGRESS" ){	ОЖИДАНИЕ( 1000 );	Документ		= МАРКИРОВКА.ЗАПРОСДОКУМЕНТА( ПРЕДПРИЯТИЕ, СЕРТИФИКАТ, ТЕСТОВЫЙКОНТУР, ОТВЕТ );	IF ( ПУСТО( Документ ) ) RETURN "";	ЗАГРУЗИТЬJSON( "Документ", Документ );		КоличествоПопыток++;}RETURN Ответ;
