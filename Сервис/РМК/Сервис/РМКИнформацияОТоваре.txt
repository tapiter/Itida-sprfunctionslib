
IF ( ДЛИНА( Товар ) < 10 )
	Товар	= ЗАПРОС( "SELECT code FROM sprres WHERE maincode= '" + Товар + "'" );
ОТПРАВИТЬСООБЩЕНИЕ( _ДЕСКРИПТОРОКНА, _СООБЩЕНИЕВЫПОЛНИТЬКОМАНДУ, "ИНФОРМАЦИЯОТОВАРАХ", ТОВАР );
