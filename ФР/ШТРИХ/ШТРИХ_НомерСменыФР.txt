// Номер смены в ФР
//IF ( ПЕРЕМЕННАЯ("НомерСменыККМ", 0) <> 0 )
//	RETURN НомерСменыККМ;

НомерСменыККМ = 0;
IF ( !ФР.ШТРИХ_ОткрытьФР( КОДОБОРУДОВАНИЯ, "Номер смены" ) ) RETURN 0;
	
driverKKM.GetECRStatus();	
НомерСменыККМ = VAL( driverKKM.SessionNumber ) + 1;

// Закрываем соединение с ККТ
ФР.ШТРИХ_ЗакрытьФРСПроверкойНаОшибки( "номер смены" );

RETURN НомерСменыККМ;
