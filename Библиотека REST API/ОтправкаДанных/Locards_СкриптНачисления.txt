IF ( МЕСТОВЫЗОВА == "ЗАПИСЬБОНУСА" )
	RETURN RESTAPI.LOCARDS_СовершениеПокупки( ИДЧЕКА, ВСЕГОБОНУСОВ );
ELSE IF ( МЕСТОВЫЗОВА == "РАСЧЕТНАЧИСЛЕНИЙ" )
	RETURN RESTAPI.LOCARDS_КоличествоБонусов( НОМЕРКАРТЫ, "" );
ELSE IF ( МЕСТОВЫЗОВА == "РАСЧЕТСКИДКИ" )
	RETURN 0;
