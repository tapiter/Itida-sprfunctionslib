IF ( МОДЕЛЬ == "МАССАКДрайверСто" )
	RETURN EQUIPMENT.ВЕСЫМАССАКДрайверСто_ПолучитьВес( КОДОБОРУДОВАНИЯ );
ELSE IF ( МОДЕЛЬ == "МАССАКДрайверР" )
	RETURN EQUIPMENT.ВЕСЫМАССАКДрайверР_ПолучитьВес( КОДОБОРУДОВАНИЯ );	
ELSE IF ( МОДЕЛЬ == "ШтрихСлим" )
	RETURN EQUIPMENT.ВЕСЫШтрихСлим_ПолучитьВес( КОДОБОРУДОВАНИЯ );	
ELSE IF ( МОДЕЛЬ == "CASADAP" )
	RETURN EQUIPMENT.ВЕСЫCASADAP_ПолучитьВес( КОДОБОРУДОВАНИЯ );		
ELSE IF ( МОДЕЛЬ == "CASCL" )
	RETURN EQUIPMENT.ВЕСЫCASCL_ПолучитьВес( КОДОБОРУДОВАНИЯ );
ELSE IF ( МОДЕЛЬ == "АТОЛ" )
	RETURN EQUIPMENT.ВЕСЫАТОЛ_ПолучитьВес( КОДОБОРУДОВАНИЯ );
ELSE IF ( МОДЕЛЬ == "Эмулятор" )
	RETURN EQUIPMENT.ВЕСЫЭмулятор_ПолучитьВес( КОДОБОРУДОВАНИЯ );	
ELSE
	RETURN 0;
