// Подготавливаем временные таблицы для заполнения их даннымиТаблицаСоответствия	= "##" + УникальноеИмя( );ТаблицаТоваров		= "##" + УникальноеИмя( );ТаблицаПоступлений	= "##" + УникальноеИмя( );ТаблицаДвижений		= "##" + УникальноеИмя( );ТаблицаОбъемов		= "##" + УникальноеИмя( );ТаблицаТоваровДляПартий		= "##" + УНИКАЛЬНОЕИМЯ( );ПодготовительнаяТаблица		= "##" + УНИКАЛЬНОЕИМЯ( );ТаблицаТоваровДляДекларации	= "##" + УНИКАЛЬНОЕИМЯ( );ЗАПРОС( "CREATE TABLE " + ПодготовительнаяТаблица + "( code char( 3 ), ic int, date datetime, client char( 7 ), sklad char ( 3 ), maincode char( 10 ), declar varchar( 50 ),													   kolp float, saldo_in float, saldo_out float, importer char( 7 ), identity_column int IDENTITY( 1, 1 ) )" );ЗАПРОС( "CREATE TABLE " + ТаблицаТоваровДляПартий + "( code char( 3 ), ic int, date datetime, client char( 7 ), sklad char ( 3 ), maincode char( 10 ), declar varchar( 50 ),													   kolp float, saldo_in float, saldo_out float, importer char( 7 ), identity_column int IDENTITY( 1, 1 ) )" );ЗАПРОС( "CREATE INDEX " + ТаблицаТоваровДляПартий + "_ic ON " + ТаблицаТоваровДляПартий + "( identity_column )" );ЗАПРОС( "CREATE INDEX " + ТаблицаТоваровДляПартий + "_maincode_date ON " + ТаблицаТоваровДляПартий + "( maincode, date )" );ЗАПРОС( "CREATE TABLE " + ТаблицаТоваровДляДекларации + "( maincode char( 10 ), importer char( 7 ),  sklad char( 3 ), ОстатокНаНачало float, ПоступлениеОтПроизводителей float, 														   ПоступлениеОтОптовиков float, ПоступлениеПоИмпорту float, ВозвратОтПокупателей float, ПрочееПоступление float,														   ПоступлениеПоПеремещению float, РасходРозница float, РасходПрочий float, РасходВозвратПоставщику float, 														   РасходПеремещение float, ОстатокНаКонец float )" );														   СИСТЕМНОЕСООБЩЕНИЕ( "Расчет остатков на начало периода." );// Формируем таблицу со списком алкогольной продукцииЗАПРОС( "CREATE TABLE " + ТаблицаТоваров + "( Подразделение varchar( 20 ), ВидПродукции varchar( 250 ), КодВидаПродукции char( 20 ), ИДПроизвИмп char( 7 ), НаименованиеПроизводителя varchar(250),											  ИннПроизводителя varchar( 20 ), КПППроизводителя varchar( 20 ), ОстатокНаНачало float, ПоступлениеОтПроизводителей float, 											  ПоступлениеОтОптовиков float, ПоступлениеПоИмпорту float, ВозвратОтПокупателей float, ПрочееПоступление float, ПоступлениеПоПеремещению float,											  РасходРозница float, РасходПрочий float, РасходВозвратПоставщику float, РасходПеремещение float,											  ОстатокНаКонец float) " );ЗАПРОС( "SELECT maincode, importer INTO " + ТаблицаСоответствия + " FROM sprnn WHERE nnvid <> ''" ); // Необходимо получить таблицу со списком производителей / импортеров// Формируем таблицу со списком производителей / поставщиковЗАПРОС( "CREATE TABLE " + ТаблицаПоступлений + "( Подразделение varchar( 20 ), ВидПродукции varchar( 250 ), КодВидаПродукции char( 20 ),  ИДПроизвИмп char( 7 ), НаименованиеПроизводителя varchar(250),												  ИннПроизводителя varchar( 20 ), КПППроизводителя varchar( 20 ), ИдПостав varchar( 10 ) DEFAULT '', КодПоставщика char( 7 ), ПризнакПоставщика int, 												  НаименованиеПоставщика varchar( 250 ), ИННПоставщика varchar( 20 ), КПППоставщика varchar( 20 ), ЛицензияНомер varchar( 250 ),												  ЛицензияДатаВыдачи datetime, ЛицензияДатаОкончания datetime, ЛицензияКемВыдана varchar( 250 ),												  ДатаПоставки datetime, НомерНакладной varchar( 30 ), НомерГТД varchar( 30 ),												  ОбъемПоставки float, ОбъемВозвратов float) " );
