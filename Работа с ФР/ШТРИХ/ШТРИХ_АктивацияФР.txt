ИНДЕКСУСТРОЙСТВАККМ	= 0;
НомерЧекаККМ 		= 0;
НомерСменыККМ 		= 0;
СменаОткрытаВККМ	= false;


IF ( !фР.ШТРИХ_ОткрытьФР( КОДОБОРУДОВАНИЯ, "Активация ФР" ) ) RETURN false;


// Если ФР находится в режиме открытого чека, то отменяем чек
driverKKM.GetECRStatus( );
IF ( driverKKM.ECRMode == 8 )
	driverKKM.CancelCheck( );

IF ( driverKKM.ECRMode == 3 )
{
	Сообщение( "Открытая смена, 24 часа кончились (необходимо закрыть смену).");
	RETURN false;
}

IF ( driverKKM.ECRMode == 5 )
{
	Сообщение( "Блокировка по неправильному паролю налогового инспектора.");
	RETURN false;
}

RETURN true;
