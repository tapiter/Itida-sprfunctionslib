//Проверим возраст буфера чеков
ВозврастБуфераДней			= 0;
НеВыводитьСообщение			= ЕСЛИ( ПУСТО( НеВыводитьСообщение ), false, НеВыводитьСообщение );

//Количество неотправленных документов:
driverKKM.FNGetInfoExchangeStatus( );
КоличествоЧеков = driverKKM.MessageCount;
	
IF ( КоличествоЧеков > 0 )
{
	// Дата самого старого неотправленного документа
	СтрокаДатаПервогоНеотправленного = "" + driverKKM.Date + " " + "00:00:00"; 
	ДатаПервогоНеотправленного = CTOD(СтрокаДатаПервогоНеотправленного,0);	
	ТекущаяДата = DATE();	
	ВозврастБуфераДней = VAL(DATEDIFF(ТекущаяДата, ДатаПервогоНеотправленного, 'day'));	
}
//конец проверки возраста буфера чеков
RETURN ВозврастБуфераДней;
