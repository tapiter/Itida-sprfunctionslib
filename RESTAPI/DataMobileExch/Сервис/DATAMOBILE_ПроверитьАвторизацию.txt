КодСотрудника = "";

IF (!ПУСТО( ТокенАвторизации ) )
{
	ДанныеАвторизации = ":" + ПЕРЕКОДИРОВАТЬ(ТокенАвторизации, "BASE64", "UTF-8");
	ЛогинАвторизации = ПОЛУЧИТЬСЛОВО(ДанныеАвторизации,":",":", 1);
	ПарольАвторизации = ПОЛУЧИТЬСЛОВО(ДанныеАвторизации,":",":", 2);
	ТЕКСТЗАПРОСА = "SELECT code FROM sprempl WHERE chektext = '" + ЛогинАвторизации + "' AND LTRIM( RTRIM( ex_code ) ) = '" + ПарольАвторизации + "'";
	
	КодСотрудника = ЗАПРОС(ТЕКСТЗАПРОСА);
}

RETURN КодСотрудника;
