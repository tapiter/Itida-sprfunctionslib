// Выплата наличных

// Параметры - 
// Сумма число вещественное, сумма вносимых наличных денег.
IF ( !ФР.АТОЛДТО10_ОткрытьФР( КОДОБОРУДОВАНИЯ, "Выплата наличных" ) ) RETURN false;

_ИМЯКАССИРА = ЗАПРОС( "SELECT name, inn FROM sprempl WHERE code = '" + froperator + "'", "name" );
_ИННКАССИРА = ЗАПРОС( "", "inn" );

ТекстЗапросаВнесение = "
{
	""type"": ""cashOut"",
	""operator"": {
		""name"": """ + _ИМЯКАССИРА + """,
		""vatin"": """ + _ИННКАССИРА + """
	},
	""cashSum"": " + СТРОКА( Сумма, 16, 2 ) + "
}
";

fptr.setParam( fptr.LIBFPTR_PARAM_JSON_DATA, ТекстЗапросаВнесение );
fptr.processJson( );
		
RETURN ФР.АТОЛДТО10_ЗакрытьФРСПроверкойНаОшибки( "Выплата наличных", true );
