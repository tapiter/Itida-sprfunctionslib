IF (TYPE("РАЗРЕШИТЬОТКЛЮЧАТЬККТ") == "U")
	РАЗРЕШИТЬОТКЛЮЧАТЬККТ = true;
	
РезультатЗакрытия		= true;
fptr = _МАССИВККТ[ VAL( ФР.КОДОБОРУДОВАНИЯ() ) ];
IF ( fptr.errorCode( ) <> 0 )
{
	// Вывод ошибки
	СООБЩЕНИЕ( "Ошибка " + fptr.errorCode( ) + " (" + МестоВызова + ")" + CHR( 13 ) + "Описание ошибки: " + fptr.errorDescription( ), "Ошибка работы с ККМ" );
	РезультатЗакрытия	= false;
	fptr.resetError( );
}

IF (РАЗРЕШИТЬОТКЛЮЧАТЬККТ)
	ФР.АТОЛДТО10_ОтключитьККТ();
	
RETURN РезультатЗакрытия;
